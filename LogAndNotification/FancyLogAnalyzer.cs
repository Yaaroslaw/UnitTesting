
namespace LogAndNotification
{
    public class FancyLogAnalyzer
    {
        public bool IsValidLogFileName(string fileName)
        {
            // read config file
            // determine if fileName is among allowed and return true if is
            return true;
        }
    }
}
